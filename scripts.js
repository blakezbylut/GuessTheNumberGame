$(function(){

  var answer = parseInt(Math.floor(Math.random()*10));

  var userInput = parseInt(prompt("Enter a number, stranger!"));

var something = function() {
  alert("Welcome");
  // while( userInput > answer ) {
  //   alert("Guess a bit lower, matey");
  //   return userInput;
  // };
  // while( userInput < answer ) {
  //   alert("Guess a bit higher, mate");
  //   return userInput;
  // };
  // while (userInput == answer) {
  //   return $('<h1>').text('You win!');
  // }
};
something();

})
